# -*- Mode: python; py-indent-offset: 4; indent-tabs-mode: nil; coding: utf-8; -*-

# def options(opt):
#     pass

# def configure(conf):
#     conf.check_nonfatal(header_name='stdint.h', define_name='HAVE_STDINT_H')

def build(bld):
    module = bld.create_ns3_module('pbq-routing', ['internet'])
    module.source = [
        'model/Ipv4-pbq-routing.cc',
        'helper/Ipv4-pbq-routing-helper.cc',
        ]

    module_test = bld.create_ns3_module_test_library('pbq-routing')
    module_test.source = [
        'test/Ipv4-pbq-routing-test-suite.cc',
        ]

    headers = bld(features='ns3header')
    headers.module = 'pbq-routing'
    headers.source = [
        'model/Ipv4-pbq-routing.h',
        'helper/Ipv4-pbq-routing-helper.h',
        ]

    if bld.env.ENABLE_EXAMPLES:
        bld.recurse('examples')

    # bld.ns3_python_bindings()

